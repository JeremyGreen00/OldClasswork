Files included:

FindComponents.c
Usage: FindComponents inputfile outputfile
Reads in Graph data and finds the strongly conneceted components

GraphTest.c
Used for testing the graph functions

List.h
Header for List ADT

List.c
ADT for integers.

Graph.h
Header for adj list for graph

Graph.c
Stores a graph in form of an adj list. Also includes BFS algorithm and required parts

Makefile
Very important. Creates exe file on the unix system

README.txt
A mysterious document that can be found everywhere.

NOTE TO GRADER:
The Makefile has been modified so every instance of GraphClient is replaced by GraphTest
(My own testing file as requiered for upload by pa5)